# Caucule a raiz quadrada informada pela usuário. Para isso utilize a biblioteca math.

import math

numero = int(input('Digite um número: '))

print(f'A raíz quadrada do número {numero} é {math.sqrt(numero):.2f}')